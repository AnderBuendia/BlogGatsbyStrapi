# BlogGatsbyStrapi

To deploy project.

Navigate into app and introduce the next command:

gatsby develop

If you need to clean the cache:

gatsby clean

To run strapi server, navigate into server folder and run this command:

npm run develop
